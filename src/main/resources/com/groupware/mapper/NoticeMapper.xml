<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.groupware.mapper.noticeMapper">
  <select id="getnoticeList" resultType="com.groupware.vo.noticeVO">
  	select * from notice
  </select>
  
  <insert id="insert">
  	insert into notice(title, content, writer) values(#{title},#{content},#{writer})
  </insert>
  
  <select id="read" resultType="com.groupware.vo.noticeVO">
  	select * from notice where notice_idx = #{noticeIdx}
  </select>
  
  <delete id="delete">
  	delete from notice where notice_idx = #{noticeIdx}
  </delete>
  
  <update id="update">
	  update
	  	notice
	  set
	  	title = #{title},
	  	content = #{content},
	  	updateDate = sysdate
	WHERE notice_idx = #{noticeIdx}
  	
  </update>
</mapper>