<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.groupware.mapper.BoardMapper">
  <select id="getBoardList" resultType="com.groupware.vo.BoardVO">
  	select * from board
  </select>

  <insert id="insert">
  	insert into board(title, content, writer) values(#{title},#{content},#{writer})
  </insert> 
  
<!--   <insert id="insertSelectKey">
  	<selectKey keyProperty="board_idx" order="BEFORE" resultType="long">
  		select board_seq.nextval from dual
  	</selectKey>
  	insert into borad(bno, title, content, writer)
  	values(#{bno}, #{title}, #{content}, #{writer})
  </insert> -->
  
  <select id="read" resultType="com.groupware.vo.BoardVO">
  	select * from board where board_idx = #{board_idx}
  </select>
  
  <delete id="delete">
  	delete from tbl_board where board_idx = #{board_idx}
  </delete>
  
<!--   <update id="update">
	  update
	  	tbl_board
	  set
	  	title = #{title},
	  	content = #{content},
	  	updateDate = sysdate
	WHERE bno = #{bno}
  	
  </update> -->
</mapper>